specVersion: "4.0"
metadata:
  name: com-sap-partner-wz-employee-directory
  copyright: |-
    * (c) Copyright 2009-${currentYear}
type: application
# workaround: prevents "File not found: base.less" error during package.zip build
framework:
  name: SAPUI5
  version: 1.136.8
  libraries:
    - name: sap.ui.core
    - name: themelib_sap_fiori_3
resources:
  configuration:
    paths:
      webapp: src
builder:
  resources:
    excludes:
     - "test/**"
     - ".card/**"