# v0.8 — PROFILES: рабочий документ

**Статус:** в работе
**Цель:** первая специализация DF — профиль как "кассета" для разных доменов.

---

## Архитектура

**Инвариант (магнитофон):** state machine, SSE, UI, file management, GitHub push.

**Профиль (кассета):** промпты агентов, deployer, verifier. Объект конфига:

```js
// server/profiles/nodejs-app.js
export default {
  id: 'nodejs-app',
  name: 'Node.js App',
  prompts: {
    architect: () => import('../prompts/architect.js'),
    developer: () => import('../prompts/developer.js'),
    tester:    () => import('../prompts/tester.js'),
  },
  deployer: 'local-runner',  // 'local-runner' | 'none'
  verifier: 'vision',        // 'vision' | 'manifest' | 'none'
}
```

**Три точки подключения:**
1. `server/index.js` — промпты агентов
2. `server/orchestrator.js` — deployer (`executeLocalDeploy` vs skip)
3. `server/orchestrator.js` — verifier (`vision` vs структурная проверка манифеста)

**Выбор профиля:** через UI перед заказом (per-order). Профиль сохраняется в state оркестратора.

---

## Integration Card — что генерируем

**Файловая структура:**
- `manifest.json` — декларация карточки (`sap.card` schema)
- `Component.js` — UI5 Component
- `i18n/i18n.properties` — переводы
- `package.json` — зависимости card SDK

**Delivery:** нет запущенного сервера → нет Local Runner. Только GitHub-архив.

**Verifier (structural):** проверяет наличие `manifest.json` и обязательных полей:
`sap.card.type`, `sap.card.header`, `sap.card.content`, `sap.app.id`.

---

## Фазы

- [ ] Phase 0: Profile infrastructure — `server/profiles/nodejs-app.js`, рефактор index.js и orchestrator.js
- [ ] Phase 1: Profile selector UI — выбор перед заказом, хранение в state
- [ ] Phase 2: Integration Card profile — промпты architect/developer/tester для карточки
- [ ] Phase 3: Pipeline адаптация — deployer:'none' (DEPLOYING мгновенно), verifier:'manifest'
- [ ] Phase 4: VerifierC — структурная проверка manifest.json
- [ ] Phase 5: Интеграционный тест + dogfooding
- [ ] Phase 6: Документация и релиз

---

## Открытые вопросы

- Тип карточки (List/Table/Object/etc.) — спрашивать в negotiate или задавать в order?
- Mock для Integration Card: нужен отдельный mock-agent-manager ответ под карточку
- Vision верификатор для карточки в sandbox — оставить Area-51 на v0.9+

---

## Решения

| Дата | Решение | Причина |
|------|---------|---------|
| 2026-05-07 | Профиль = кассета (промпты + deployer + verifier) | Три точки подключения уже есть, нужно сделать явными |
| 2026-05-07 | Выбор профиля per-order, не per-instance | Proto DF должен обслуживать разные домены в одном экземпляре |
| 2026-05-07 | deployer:'none' → DEPLOYING мгновенно, state machine не меняется | Консистентность pipeline, не ломаем UI |
| 2026-05-07 | Integration Card verifier = проверка manifest.json | Без running server — только структурные проверки |
