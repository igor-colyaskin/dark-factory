# v0.7 — VERIFY: рабочий документ

**Статус:** в работе
**Цель:** агент проверяет своё приложение глазами заказчика и выдаёт отчёт.

---

## Архитектура

- **VerifierA** (структурный): HTTP-проверки, HTML, роуты — быстро, бесплатно
- **VerifierB** (vision LLM): puppeteer-core скриншот + Hyperspace vision-модель
- **Compositor**: `verifier.run(url, spec) → Report` — A → B, graceful degradation при ошибке B
- **Контракт:** форма Report выводится из реализации VerifierA

Оркестратор вызывает один `verifier.run()` и не знает о внутренней структуре.

---

## Фазы

- [x] Phase 0: Hyperspace image support — тестовый скрипт
- [x] Phase 1: VerifierA — HTTP + HTML структурные проверки, форма Report
- [x] Phase 2: VerifierB — puppeteer-core скриншот + vision LLM
- [ ] Phase 3: Compositor — `verifier.run()`, A→B, graceful degradation
- [ ] Phase 4: Orchestrator — состояние VERIFYING, хранение Report в state
- [ ] Phase 5: UI — строка Verification в таблице, отображение Report, stage description
- [ ] Phase 6: Интеграционный тест + dogfooding
- [ ] Phase 7: Документация и релиз

---

## Открытые вопросы

- Hyperspace image support: ✅ оба — gemini-2.5-flash и claude-sonnet. Формат data:image/png;base64 работает.
- Какой уровень детализации Report нужен в UI? (решить по результатам Phase 1)

---

## Решения

| Дата | Решение | Причина |
|------|---------|---------|
| | | |
