# DF-IC Demo — заметки для питча

Аудитория: тимлид. Формат: показываю сам, в конце — "хочешь попробовать?".

---

## Ключевые тезисы

### DF-IC + Vibe coding = полный workflow

DF-IC покрывает воспроизводимые 80%: структура карточки, маппинг полей,
mock-данные, тесты, sandbox preview — за ~1 минуту вместо ~2 часов вручную.

Для оставшихся 20% — специфика команды, нестандартные паттерны,
индивидуальные требования — вайб-кодинг на готовом скаффолде работает
быстрее и надёжнее, чем пытаться описать всё это в промпте.
Это не ограничение — это дизайн-принцип: правильное разделение труда
между генерацией и кастомизацией.

---

## Pitch — draft-v2

### Вступление (согласовано, требует стилистической доводки)

> *«В работе есть задачи, где нужно думать — и есть рутина, где работают руки.
> Казалось бы, именно там ошибиться сложнее. Но на практике — наоборот:
> как раз рутинная, воспроизводимая работа чаще всего становится источником
> ошибок из-за человеческого фактора. DF-IC создана для этого.
> То, на что раньше уходило два часа, — теперь минута. Ошибки исключены.»*

Оба аргумента (скорость + корректность) выходят из одного setup-а — не надо выбирать.

---

### Структура питча (v2)

```
1. Вступление     [текст выше — тема]
                   Anchor: «Пример прямо сейчас — создание IC.»
                   [сразу запускаем, без лишних слов]

2. Block 1        Mini-intro: «пишу заказ → запускаем»
   Generate       [DEMO: generate с мокапом, ~5 мин]
                   Punch line: «90 секунд. Вручную — полтора часа.»

3. Block 2        Mini-intro: «нужна вторая версия под другой backend.
   Clone          Обычно — клонируешь папку руками, потом час ищешь,
                   где не заменил namespace. Вот что происходит здесь.»
                   Aside (30 сек): import — «готовую карточку можно принести так»
                   [DEMO: clone → открываем, видим чистый rename, ~2 мин]
                   Punch line: «Детерминированно. Без LLM. Без ошибок.»

4. Горизонт       «Сегодня — карточки. Но рутинная воспроизводимая
                   работа есть везде.» (~30 сек)

5. Закрытие       «Это уже работает — я использую сам.
                   Следующий шаг — предложить команде.
                   Как смотришь?»
                   Throwaway (с улыбкой):
                   «Польза прямая: меньше времени, меньше ошибок.
                   И строчка в развитие — честная,
                   не высосанная из пальца.»
```

**Цепочка для демо:** generate → import той же карточки → clone.
Единый нарратив, не нужны реальные данные из проекта.

**Namespace-ошибки:** не во вступлении — в mini-intro к Block 2 (Clone),
после того как тимлид увидел сгенерированную карточку.

---

### Открытые вопросы (v2)

- **Горизонт:** конкретику не называть — «рутинная воспроизводимая работа есть везде» достаточно. Тимлид сам додумает.
- ~~Закрытие~~ ✅ закрыто

---

## Задел на draft-v3 (материал, содержание ещё зреет)

### Горизонт — уточнённый фрейм

Абстрактное «паттерн универсальный» заменить на конкретную боль команды:

> *«Следующая дыра — документация. Сейчас это ручная работа без правил:
> каждый пишет что-то своё, или не пишет вообще. Паттерн тот же —
> воспроизводимая работа, которую легко стандартизировать.»*

**Почему это сильнее:** тимлид не чувствует эту боль лично, но отвечает
за команду. Аргумент — не «тебе неудобно», а «твоя команда тратит время
на работу без критериев». Это его зона ответственности.

### Сервис changelog / документации (будущий тайл)

Идея: просмотреть последние коммиты карточки → LLM резюмирует изменения →
обновляет README (версия N, что изменилось) + генерирует фрагмент для Confluence
рядом с карточкой (copy-paste вручную, 30 сек).

Реализация: самодостаточная, без внешних API. После демо — чистая задача в бэклог.

### Анекдот для mini-intro Block 2

История с тимлидом, который вручную проставлял version bump после своих правок —
использовать как вербальный пример человеческого фактора. Не демо-сценарий
(функции нет), просто живая иллюстрация тезиса.

---

- [ ] Тщательно продумать сценарий Block 1 — Generate: конкретный заказ, мокап, точный текст, хронометраж
- [ ] Тщательно продумать сценарий Block 2 — Clone: цепочка import → clone, что именно показываем в результате

---

## Сценарии для демо

### Сценарий A — Generate с мокапом (primary, ~5 мин)

Реальная карточка из проекта, скриншот мокапа.
Пишем описание → запускаем → тимлид смотрит пайплайн → Preview в sandbox.

**Punch line:** *«Это заняло 90 секунд. Вручную — полтора часа.»*

**Риски:** LLM медленный / плохой результат → fallback (сценарий B).

**TODO:**
- [ ] Подобрать конкретную карточку — реальную из проекта, которую тимлид узнает
- [ ] Записать точный текст заказа — без импровизации
- [ ] Прогнать 2–3 раза, убедиться что генерирует хорошо
- [ ] Хронометраж полного сценария

### Сценарий B — Clone (backup, ~1 мин)

Клонируем готовую карточку, меняем slug.
Показываем: namespace переименован детерминированно, без LLM, без ошибок.

**Когда использовать:** если A подвёл, или как быстрый второй акт.

### Что не показывать в питче

- Edit mode — слишком много контекста для первого показа
- Сравнение моделей (Gemini vs Claude) — это внутренняя задача, не для тимлида
- Import — не добавляет к основному message

---

## Подготовка к демо

- [ ] Fallback: предзагенеренная карточка в My Apps на случай сбоя
- [ ] Один конкретный инцидент с human error (namespace / клонирование) — для подкрепления тезиса
- [ ] Определить следующий шаг после «да» — дать доступ? Попробовать на его карточке?

---

## Тесты (до финального выбора сценариев)

### Сравнение моделей vision (UX-008)

Один и тот же мокап, обе модели, одинаковый промпт:

| Метрика | Gemini 2.5 Flash | Claude Sonnet 4.6 |
|---------|-----------------|-------------------|
| Токены (input) | | |
| Токены (output) | | |
| Время (сек) | | |
| Стоимость ($) | | |
| Качество анализа | | |

Качество оценивать по: полнота списка полей, точность определения viewControl,
наличие ложных полей, читаемость результата для Архитектора.

### Лог тестовых прогонов

_Записывать по мере тестирования: сценарий / мокап / модель / результат / годен для демо (да/нет/с оговорками)_

---

## Открытые вопросы

- Какой конкретный инцидент с human error назвать?
- Что предложить после «да» — доступ к серверу или показ на его карточке?
